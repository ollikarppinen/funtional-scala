package week4
import week4._

object nth {
  def nth[T](n: Int, xs: List[T]): T =
    if (n == 0) xs.head
    else nth(n - 1, xs.tail)

  val list = List
  
  list.apply(1)
  
  list.apply(1, 2)

  println(list.apply(1, 2, 3))
}